<ul class="list-unstyked">
    <li>表示名<%= current_user.name %></li>
    
    <% if current_user.gender==1 %>
      <li>性別 男性</li>
    <% else %>
      <li>性別 女性</li>
    <% end %>
    <li>年齢<%= current_user.age %></li>
    <li>居住区<%= current_user.prefecture %></li>
    <li>コメント<%= current_user.description %></li>
</ul>
<%= link_to 'ユーザ情報を変更', edit_user_path(current_user.id) %>
